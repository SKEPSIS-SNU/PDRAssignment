import { Loader2 } from "lucide-react";

const Loader = () => {
  return (
    <main className="min-h-screen fl_center">
      <Loader2 className="animate-spin text-primary w-10 h-10" />
    </main>
  );
};

export default Loader;
